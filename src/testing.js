// execute test for all operations
const { operatorsHandlers } = require("./operatorsHandlers");

const operatorsList = [
  "$match",
  "$format",
  "$groupBy",
  "$sort",
  "$limit",
  "$skip",
  "$unwind",
  "$remove",
];
